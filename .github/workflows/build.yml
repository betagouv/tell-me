name: Build

on: push

jobs:
  docker:
    name: Docker
    runs-on: ubuntu-latest
    env:
      PORT: 3000
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: docker-compose build app
